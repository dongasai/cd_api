# 审计日志模型流转记录优化

## Context

**需求**: 完整记录模型名称在请求处理流程中的变化轨迹

**字段规划**:
- `model` - 用户原始请求模型名（不变）
- `actual_model` - 渠道响应返回的模型名（语义变更）
- `apply_data` (新增) - 中间过程数据：
  - `matched_models`: 参与匹配的模型列表（别名扩展结果）
  - `channel_request_model`: 发送给渠道的模型名

**方案**: 渐进式记录方案
- 选择渠道阶段记录 apply_data
- Handler 处理响应后更新 actual_model

## Implementation Plan

### Phase 1: 数据库层
1. 创建迁移文件：新增 apply_data JSON 字段
2. 更新 AuditLog 模型：添加到 fillable 和 casts

### Phase 2: 响应层
3. StreamHandler：提取模型名，更新审计日志逻辑
4. NonStreamHandler：记录渠道响应模型名

### Phase 3: 服务层
5. ProxyServer：选择渠道后记录 apply_data
6. ChannelRouterService：确保方法可访问

### Phase 5: 测试验证
7. 编写单元测试
8. 功能测试验证
9. 代码格式化

## Status

进行中...