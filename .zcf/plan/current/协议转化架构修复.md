# 协议转化架构修复计划

## 任务描述
修复协议转化的架构错误，使 SharedDTO 成为纯数据容器，转换职责由 Protocol Driver 层承担。

## 执行步骤

### Phase 1: Protocol Driver 层修改
- [ ] `OpenAI/Tool.php`: 添加 `fromSharedDTO()`, `toSharedDTO()`，删除跨协议检测
- [ ] `OpenAI/ToolCall.php`: 添加 `fromSharedDTO()`, `toSharedDTO()`
- [ ] `Anthropic/Tool.php`: 添加 `fromSharedDTO()`, `toSharedDTO()`，删除跨协议检测
- [ ] `OpenAI/ContentPart.php`: 修改 `fromSharedDTO()` 直接读取属性
- [ ] `Anthropic/ContentBlock.php`: 修改 `fromSharedDTO()` 直接读取属性
- [ ] `OpenAI/Message.php`: 修改对 SharedContentBlock::fromOpenAI() 的调用

### Phase 2: SharedDTO 层删除方法
- [ ] `Request.php`: 删除 `toOpenAI()`, `toAnthropic()`, `toOpenAIFormat()`, `toAnthropicFormat()`
- [ ] `Message.php`: 删除 `toOpenAI()`, `toAnthropic()`
- [ ] `ContentBlock.php`: 删除 `toOpenAI()`, `toAnthropic()`, `fromOpenAI()`, `fromAnthropic()`
- [ ] `ToolCall.php`: 删除 `toOpenAI()`, `toAnthropic()`, `fromOpenAI()`, `fromAnthropic()`
- [ ] `Usage.php`: 删除 `toOpenAI()`, `toAnthropic()`, `fromOpenAI()`, `fromAnthropic()`
- [ ] `Response.php`: 删除 `toOpenAI()`, `toAnthropic()`

### Phase 3: 枚举层处理
- [ ] `FinishReason.php`: 删除 `toOpenAI()`, `toAnthropic()`, `fromOpenAI()`, `fromAnthropic()`

### Phase 4: Provider 层适配
- [ ] 修改所有使用 `fromOpenAI()`/`fromAnthropic()` 的地方

### Phase 5: 测试修复
- [ ] 更新单元测试

## 开始时间
2026-03-24 02:30